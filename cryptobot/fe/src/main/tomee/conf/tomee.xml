<?xml version="1.0" encoding="UTF-8"?>
<tomee>
    <Resource id="cryptodb" type="DataSource">
        JdbcDriver ${database.driver.name}
        JdbcUrl ${database.connection.url}
        UserName ${database.user}
        Password ${database.password}
        JtaManaged true
    </Resource>
</tomee>